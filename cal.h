#include <stdio.h>

/*The function gets the operator input by user then returns that operator*/
char getOperator(void);

/*The function prompts user to input a number then returns that number */
double getNumber(void);

/* The function removes input buffer (if any) */
void clearBuffer(void);
